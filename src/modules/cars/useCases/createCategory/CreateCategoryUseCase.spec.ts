describe("Create Category", () => {
  it("User should be abe sum 2+2 = 4", () => {
    const sum = 2 + 2;
    const result = 4;

    expect(sum).toBe(result);
  });

  it("Espero que 2+2 não seja 5", () => {
    const sum = 2 + 2;
    const result = 5;

    expect(sum).not.toBe(result);
  });
});
